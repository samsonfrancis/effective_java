package com.sam.effective_java.write_docs_comments;

/**
 * Below is an example of comments for generic types
 * <p>
 * An object that maps keys to values. A map connot contain duplicate keys; each key can map at most on values.
 * <p>
 * (Remainder omitted)
 *
 * @param <K> the type of keys maintained by this map
 * @param <V> the type of mapped values
 */
public interface Map<K, V> {
    //remainder is omitted
}
